Ничего космического в рамках тестового задания я не создал, но все же обращу внимание на ряд
моментов, которые может стоит учесть при оценивании его.

Постарался соблюсти схему разделения данных приложения MVC

Минимум дублирования кода( если оно есть вообще)

Можно было бы покрыть весь код ексепшенами, но я лишь добавил пару, чтобы было понятно что я в курсе об их существовании

Где то немного jquery

Обмен данными с клиентом посредством ajax( при этом единичный экземпляр кода, универсальный для всех контроллеров и форм)

Не использовал синтаксис из старых версий php, котоырй вызывает нотисы и варнинги, и при это не внедрял операторы которые работают только на 7.3+ и вызывают фатал на 5.6

Внедрил механизм валидации, который написал сам, он попроще и не такой классный как в современных
фрейфворках, но в то же время не валидация через IF ELSE миллионом строк при обработке каждой формы.

С версткой не сильно возился, так как и не нужно было, но все же более менее удобно на мой взгяд.

Постаралься создать какой-никакой уровень абстракции, при этом не углубляясь через чур.

прошу прощения за наименования файлов, общего стиля не выдержано, так как начал по-одному, потом переделывал на другой лад, но гит что то не схватил изменение табуляции, не стал тратить время на выяснение почему, это сейчас не так важно.